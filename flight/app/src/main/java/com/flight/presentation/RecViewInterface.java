package com.flight.presentation;

public interface RecViewInterface {
    //Interface is a contract between any class that implements it and makes sure that class has
    //whatever methods are listed in that interface.

    void onItemClick(int position);
}
