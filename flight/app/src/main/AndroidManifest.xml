<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flight">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flight">

        <activity
            android:name=".presentation.ReservationFormActivity"
            android:exported="false"
            android:label="Reservation"
            android:parentActivityName=".presentation.ReservationFormActivity"/>

        <activity
            android:name=".presentation.RecFlightsActivity"
            android:exported="false"
            android:label="Flights"
            android:parentActivityName=".presentation.MainActivity" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>